


login do paciente

armazenar os dados do login desse id. e verificar qual o tipo (ja faz isso)

{esse passo pode ser reusado nos (tabela medicos)}
[verificar qual id_usuario (tabela paciente) == id (tabela usuario), e retornar esse item da
 tabela paciente
]

verificar na (tabela consulta) e retornar todos
 os items para listagem em que id_paciente (consulta) == id (paciente)


url do tipo paciente (listarUsuarioTipo)
listar paciente (listarPaciente)
listar medico (listarMedico)
